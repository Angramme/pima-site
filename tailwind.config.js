const colors = require('tailwindcss/colors');

module.exports = {
  darkMode: "media",
  content: [
    // ..
  ],
  theme: {
    extend: {
      colors: {
        ...colors,
      }
    },
  },
  plugins: [],
};
